# Conversor de Medidas CGI (Python)

Digite "py -m http.server 8080 --cgi" (sem as áspas) em seu terminal para iniciar o servidor

Para acessar o Conversor de Medidas, acesse http://localhost:8080 em seu navegador
